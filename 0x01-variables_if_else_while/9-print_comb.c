#include <stdio.h>
/**
 * main - entry point
 *
 * Description: print all single digit
 *
 * Return: Alwayes 0 (Success)
*/
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
		if (n != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
